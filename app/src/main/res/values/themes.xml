<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IMADAsignment2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/theme_green</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/theme_blue</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/background_light</item>

        <!-- Professional Font -->
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="Theme.IMADAsignment2" parent="Base.Theme.IMADAsignment2" />
</resources>